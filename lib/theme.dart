import 'package:flutter/material.dart';

final darkColorScheme = ColorScheme.fromSeed(
  seedColor: Color.fromARGB(255, 4, 3, 71),
);

final theme = ThemeData().copyWith(colorScheme: darkColorScheme);
